{% extends 'base.html.twig' %}

{% trans_default_domain "CzesioNestablePageBundle" %}

{% block body %}
    <h1>PageMeta creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('czesio_pagemeta_index') }}">{{ 'back_to_list' | trans }}</a>
        </li>
    </ul>
{% endblock %}
